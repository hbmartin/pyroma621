[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "readme-type-mismatch"
version = "1.0"
readme = {file = "README.md", content-type = "text/x-rst"}
