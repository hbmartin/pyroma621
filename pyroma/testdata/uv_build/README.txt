This is the readme for the uv_build test package for pyroma.

It exists to verify that pyroma can extract metadata from a project that
uses uv's native PEP 517 build backend, uv_build, just like it can for
setuptools, flit and any other standards-compliant backend. The text here
needs to be over a hundred characters, so that the Description check in
pyroma passes for this package.
