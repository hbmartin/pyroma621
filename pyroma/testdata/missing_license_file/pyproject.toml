[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "missing-license-file"
version = "1.0"
license = "MIT"
license-files = ["LICENCE-DOES-NOT-EXIST*"]
