[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.ruff]
line-length = 120
extend-exclude = ["pyroma/testdata"]

[tool.ruff.lint]
extend-select = ["B", "I", "UP"]
